import {
    Container,
    SubContainer,
} from "./styledForm";

const Form = () => {
    return (
        <Container>
            <SubContainer>
                Form
            </SubContainer>
        </Container>
    );
};

export default Form;
