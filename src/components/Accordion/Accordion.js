import {
    Container,
} from "./styledAccordion";

const Accordion = () => {
    return (
        <Container>
            A Simple Accordion
        </Container>
    );
};

export default Accordion;
